#!/bin/bash
# © 2015 Stsosz. All Rights Reserved.
# Set Port module for AresBD by Stsosz
source ~/Ares/aresbd.conf
attackShell()
{
	if [[ -z "$PORT" ]]; then
		echo "You need to set the port first!"
	else
		nc -l -p $PORT
	fi
}